gl_dep = dependency('gl')
glfw_dep = dependency('glfw3')
glew_dep = dependency('glew')
glm_dep = dependency('glm')

executable(
  'sandbox',
  'sandbox.cpp',
  dependencies: [omelette_dep, gl_dep, glfw_dep, glew_dep, glm_dep],
)
